apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ia-clair.fullname" . }}
  labels:
    {{- include "ia-clair.labels" . | nindent 4 }}
data:
  {{- if .Values.env }}
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
  {{- end }}