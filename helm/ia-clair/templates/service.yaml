apiVersion: v1
kind: Service
metadata:
  name: {{ include "ia-clair.fullname" . }}
  labels:
    {{- include "ia-clair.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ia-clair.selectorLabels" . | nindent 4 }}
